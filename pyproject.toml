[project]
name = "agent-experiment"
version = "0.1.0"
description = "Offline experiment delivery assistant agent for course Experiment 2."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
