[project]
name = "agent-experiment"
version = "0.1.0"
description = "课程实验二的离线实验交付助手 Agent。"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
